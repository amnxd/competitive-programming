#pragma GCC optimize ("O3")
#include <bits/stdc++.h>

int main() {
    std::cin.tie(0)->sync_with_stdio(0);
    int t;
    std::cin >> t;

    while(t--) {
        int n; 
        std::cin >> n;

        std::string s;
        std::cin >> s;

        if(s[0] == '0') {
            std::cout << "-1\n";
            continue;
        }

    }
}