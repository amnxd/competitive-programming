#include <bits/stdc++.h>
#pragma GCC optimize ("O3")

int main() {
    std::cin.tie(0)->sync_with_stdio(0);
    int t;
    std::cin >> t;

    while(t--) {
        int n; 
        std::cin >> n;

        $0

    }
}